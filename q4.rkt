#lang racket
(provide (all-defined-out))

;; Add a contract here
(define shift-left
  (lambda (ls)
    "Implement me"))

;; Add a contract here
(define shift-k-left
  (lambda (ls k)
    "Implement me"))

;; Add a contract here
(define shift-right
  (lambda (ls)
    "Implement me"))

;; Add a contract here
(define combine
  (lambda (ls1 ls2)
    "Implement me"))

;; Add a contract here
(define sum-tree
  (lambda (tree)
    "Implement me"))

;; Add a contract here
(define inverse-tree
  (lambda (tree)
    "Implement me"))